model User {
  id        Int      @id @default(autoincrement())
  fullName  String
  email     String   @unique
  password  String
  cart      Cart[]
  orders    Order[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
